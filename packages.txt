# Curated list of top-level packages for the IDE's self-hosted repo.
#
# These are relocated from Termux's *prebuilt* packages (see relocate.py)
# instead of being rebuilt from source, so each name must match a real
# package in termux-main. Transitive dependencies are resolved automatically
# by relocate.py, so only top-level `pkg install` targets are listed.

# --- Bootstrap base (generate-bootstraps.sh essentials) ---
apt
bash
bzip2
command-not-found
coreutils
curl
dash
diffutils
findutils
gawk
grep
gzip
less
procps
psmisc
sed
tar
termux-core
termux-exec
termux-keyring
termux-tools
util-linux
xz-utils
ed
debianutils
dos2unix
inetutils
lsof
nano
net-tools
patch
unzip

# --- Base / IDE setup (idesetup) ---
jq
wget
libcurl
openssh
git
openjdk-17
openjdk-21

# --- C/C++ / native toolchain ---
clang
lld
llvm
libcompiler-rt
cmake
ninja
make
ndk-sysroot

# --- Rust + runtime deps ---
rust
ca-certificates
openssl
libxml2
zstd
ncurses
libffi
libiconv
libicu
zlib
libandroid-execinfo
libandroid-glob

# --- Scripting ---
python
